spring:
  # 추가
  session:
    store-type: jdbc
  profiles:
    include: real-db
  # 추가 끝


  jpa:
    properties:
      hibernate:
        # 추가
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        # 추가 끝

server:
  port: 9090