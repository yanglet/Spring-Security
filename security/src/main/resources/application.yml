spring:
  datasource:
    url: jdbc:mysql://localhost:3306/calendarservice
    username: yanglet
    password: yanglet
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace